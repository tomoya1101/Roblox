--// TynouDypak \\--
--// i just changed some sounds n added a lock \\--

-- if you wish to change the key name, you can do so BUT you must change the
-- 'value' (under the 'KeyName' under the 'Unlock' script) to the new key name
-- you can also remove the key from this group for easier access of moving it, it
-- still works propery.